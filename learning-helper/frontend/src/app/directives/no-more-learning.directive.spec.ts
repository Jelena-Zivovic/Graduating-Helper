import { NoMoreLearningDirective } from './no-more-learning.directive';

describe('NoMoreLearningDirective', () => {
  it('should create an instance', () => {
    const directive = new NoMoreLearningDirective();
    expect(directive).toBeTruthy();
  });
});
